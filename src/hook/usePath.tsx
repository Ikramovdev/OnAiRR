interface PathType {
    movies: string,
    series: string,
    channels: string
    music: string
}
export const PATH: PathType = {
    movies: '/',
    series: '/series',
    channels: '/channels',
    music: '/music'
}